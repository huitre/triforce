/*
 * @array
 * @array
 * @array
 */
var Matrice = function () {
	this._position = new Vector2d(0, 0, 0);
}

Matrice.prototype.add = function(first_argument) {
	// body...
};

Matrice.prototype.sub = function(first_argument) {
	// body...
};

Matrice.prototype.mul = function(first_argument) {
	// body...
};